# import torch
# import torch.nn as nn
# import torch.nn.functional as F
# from torch.autograd import Variable
# import numpy as np
# import math
# import os

index = []
a = [0]
for i in range (0, 10):
    a = list((i, ))
    index.append(a)
print(index[:][:])